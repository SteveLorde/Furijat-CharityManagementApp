import { FilterbycharityidPipe } from './filterbycharityid.pipe';

describe('FilterbycharityidPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycharityidPipe();
    expect(pipe).toBeTruthy();
  });
});
