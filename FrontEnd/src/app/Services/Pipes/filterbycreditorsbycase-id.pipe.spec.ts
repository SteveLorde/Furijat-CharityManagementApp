import { FilterbycreditorsbycaseIDPipe } from './filterbycreditorsbycase-id.pipe';

describe('FilterbycreditorsbycaseIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycreditorsbycaseIDPipe();
    expect(pipe).toBeTruthy();
  });
});
