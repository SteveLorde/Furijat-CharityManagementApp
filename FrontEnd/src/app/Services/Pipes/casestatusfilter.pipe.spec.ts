import { CasestatusfilterPipe } from './casestatusfilter.pipe';

describe('CasestatusfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CasestatusfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
