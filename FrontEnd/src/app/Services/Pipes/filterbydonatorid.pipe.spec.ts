import { FilterbydonatoridPipe } from './filterbydonatorid.pipe';

describe('FilterbydonatoridPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbydonatoridPipe();
    expect(pipe).toBeTruthy();
  });
});
