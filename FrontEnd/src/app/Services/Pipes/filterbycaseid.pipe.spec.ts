import { FilterbycaseidPipe } from './filterbycaseid.pipe';

describe('FilterbycaseidPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycaseidPipe();
    expect(pipe).toBeTruthy();
  });
});
